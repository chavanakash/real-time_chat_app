apiVersion: v1
kind: Service
metadata:
  name: chat-app-service
spec:
  type: NodePort
  selector:
    app: chat-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30001   # Optional: pick any available port between 30000–32767
